# DEEPSEEK_API_KEY: Required for DeepSeek API calls.
# Configure this secret in the AI Studio UI Secrets panel or in your runtime environment.
DEEPSEEK_API_KEY="sk-aed93dc21063480fb578946b634e0fac"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="https://api.deepseek.com"
